package sdu.splitit

import android.os.Bundle
import android.widget.Button
import android.widget.TextView
import android.widget.Toast
import androidx.appcompat.app.AppCompatActivity

class ProfileActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_profile)

        // Accessing the views using the IDs from your layout
        //val nameTextView = findViewById<TextView>(R.id.name)
        //val professionTextView = findViewById<TextView>(R.id.profession)
        //val bioTextView = findViewById<TextView>(R.id.bio)
        //val followButton = findViewById<Button>(R.id.followButton)
        //followButton.setOnClickListener {
            // Show a message when the follow button is clicked
            //Toast.makeText(this, "Followed ${nameTextView.text}", Toast.LENGTH_SHORT).show()
        }

        //messageButton.setOnClickListener {
            // Show a message when the message button is clicked
            //Toast.makeText(this, "Message sent to ${nameTextView.text}", Toast.LENGTH_SHORT).show()
        }
    //}
//}
